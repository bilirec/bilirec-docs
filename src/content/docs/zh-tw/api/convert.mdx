---
title: 轉換任務
description: MP4 轉換任務查詢與取消 API
---

啟用 `CONVERT_TO_MP4` 後，錄製完成的可轉換檔案（FLV/TS）自動加入轉換隊列。

## 列出進行中的任務

```http
GET /convert/tasks
```

## 取消任務

```http
DELETE /convert/tasks/:task_id
```

- `204 No Content` — 取消成功
- `404` — 任務不存在

轉換設定詳見 [轉換設定](/zh-tw/configuration/conversion/)。
