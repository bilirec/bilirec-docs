---
title: FRP 設定
description: FRP 內網穿透參數、協議約束與設定示例
---

import { Aside } from '@astrojs/starlight/components';

啟用 FRP (`FRP_ENABLED=true`) 時，bilirec 作為內網穿透客戶端連接到 FRP 服務器。

僅設置 `FRP_ENABLED=true` 時，程序默認使用官方免費公共 FRP 服務；設置 `FRP_SERVER`、`FRP_BASE_DOMAIN` 或 `FRP_TOKEN` 時切換為自定義設定。

## 基礎參數

| 變量 | 說明 | 默認值 |
|------|------|--------|
| `FRP_ENABLED` | 是否啟用 FRP | `false` |
| `FRP_SERVER` | FRP 服務器地址（`host:port`） | `tunnel.bilirec.org:7000` |
| `FRP_TOKEN` | FRP 認證 Token | (空) |
| `FRP_BASE_DOMAIN` | 公網基礎域名 | `tunnel.bilirec.org` |
| `TRUSTED_PROXIES` | 受信任代理列表 | `161.33.159.26` |

## 協議設定

| 變量 | 說明 | 默認值 |
|------|------|--------|
| `FRP_HTTPS` | FRP 代理協議（`true`=HTTPS） | `false` |
| `FRP_SCHEME_HTTPS` | 公網 URL scheme | `true` |

<Aside type="caution">
**協議約束：**
- `FRP_ENABLED=true` 且 `FRP_HTTPS=true` 時，**必須**同時設置 `SERVER_CRT` + `SERVER_KEY`
- 同時設置 `SERVER_CRT` + `SERVER_KEY` 且 `FRP_ENABLED=true` 時，`FRP_HTTPS` **不能**為 `false`

違反約束會導致協議不匹配，程序在啟動階段直接報錯退出。
</Aside>

## 設定示例

**官方公共服務（推薦）：**

```bash
export FRP_ENABLED=true
```

**自定義 FRP：**

```bash
export FRP_ENABLED=true
export FRP_SERVER=your-frp-ip:7000
export FRP_TOKEN=your-token
export FRP_BASE_DOMAIN=your-domain.com
export FRP_HTTPS=false
export FRP_SCHEME_HTTPS=true
```

## 啟動日誌

```
FRP enabled in official-public mode    # 官方公共服務
FRP enabled in custom-selfhost mode    # 自定義 FRP
```

操作流程見 [遠程訪問指南](/zh-tw/guides/frp/)。
